apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rancher-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "rancher-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.nginx.service.port }}:{{ .Values.nginx.service.port }}']
  restartPolicy: Never
